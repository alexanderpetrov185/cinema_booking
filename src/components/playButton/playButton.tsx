import React from 'react';
import "./playButton.scss"

const PlayButton = () => {
    return (
        <button className={"playButton"}>
            PLAY
        </button>
    );
};

export default PlayButton;